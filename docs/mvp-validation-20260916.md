# LINGOU 一期 MVP 修复后验证报告

运行编号：`mvp-20260916-02`

执行日期：2026-09-16

源码基线：`a68b2bffd734eaaca658d380fd364f91c9733afa` 加
[manifest.json](validation/mvp-20260916-02/manifest.json) 中的未提交源码 SHA-256 快照。

## 结论

**第 11 步发现的 13 项缺陷已修复并通过对应回归，但完整 MVP 仍不能判定通过。**

[120 条测试表](mvp-test-execution.csv)的最新结果：

| 范围 | 通过 | 失败 | 阻塞 | 未执行 |
|---|---:|---:|---:|---:|
| 当前 MVP（第 01-11 步，110 条） | 47 | 0 | 63 | 0 |
| 其中 P0（65 条） | 34 | 0 | 31 | 0 |
| 其中 P1（45 条） | 13 | 0 | 32 | 0 |
| 第 12 步扩展候选（6 条） | 0 | 0 | 0 | 6 |
| 最终硬件后置（4 条） | 0 | 0 | 0 | 4 |
| 合计 | 47 | 0 | 63 | 10 |

当前已没有已知失败用例，但仍有 31 条 P0 阻塞。阻塞项需要 T3 独立部署、
真人连续语音、真实 H5 音频、移动端、网络故障或最终硬件环境，不能由隔离测试替代。
第 12 步未开始。

## 缺陷关闭

| 缺陷 | 修复结果 | 验证 |
|---|---|---|
| QA-001 | 严格校验 24 kHz、mono、s16le、偶数字节及 4096 字节上限 | PROTOCOL-03 |
| QA-002 | 按 audio_id 跟踪 chunk_count 和下一序号，拒绝缺失、重复、乱序及变更 | PROTOCOL-04 |
| QA-003 | 仅首次 listening 状态建立 session，旧 session 音频只消费不播放 | PROTOCOL-05 |
| QA-004 | 为取消的 session/turn/audio 建立墓碑，安全丢弃 descriptor 后和中段迟到帧 | CANCEL-02/03 |
| QA-005 | stop_audio/turn_cancelled 必须匹配当前 session 和 turn | CANCEL-05 |
| QA-006 | 正常与异常关闭统一执行指数退避，4410 继续保持显式恢复 | RECOVER-03、CANCEL-06 |
| QA-007 | 服务端回执严格执行 decoded -> playback_started -> terminal，终态不可冲突 | PROTOCOL-06 |
| QA-008 | 每条设备上行前复验原凭据、base 和 owner，失权立即取消并关闭 4401 | DEVAUTH-05 |
| QA-009 | 输出流构造失败时关闭已创建的输入流 | CARRIER-07 |
| QA-010 | POSIX stdin 改用事件循环 add_reader，不再遗留阻塞线程 | CARRIER-07 |
| QA-011 | 连接重置时清空 final/reply/audio_id 和完成事件 | RECOVER-08 |
| QA-012 | 声卡播放移入独立可取消队列，控制消息不再等待阻塞写入 | PROTOCOL-08 |
| QA-013 | WebSocket 关闭限时 2 秒，并在 ASR provider ready 后才启动声学测试输入 | CARRIER-01、声学复验 |

## 自动验证

- Python：`142/142` 通过，其中后端 `109`、硬件适配器/固件合同 `33`。
- 前端：认证、主路径、通话生命周期、试听四组探针通过。
- 前端生产构建：通过，Vite 转换 64 个模块。
- `pip check`、Python 编译和 `git diff --check`：通过。
- API 受控验收：`19/19` 通过。
- PCM、取消、恢复受控探针：`28/28` 通过。
- 凭据、回执、资源退出探针：`9/9` 通过。
- 30 秒无播放回执正确进入 `audio_failed`。
- 60 秒 4410 替换期间没有自动抢回，显式恢复后才建立第二条连接。
- 400 ms 慢播放时，stop_audio 在 100 ms 观察窗口内生效。

完整结果见[验证证据目录](validation/mvp-20260916-02/)。

## 实际音频

- MacBook Pro 输入设备 `0`、输出设备 `1`。
- 16 kHz 单声道输入共采集 25 帧，每帧 640 字节，总计 16,000 字节，峰值 1333。
- 24 kHz 单声道输出成功写入 4,800 字节。
- 最终显式设备索引声学轮次：
  - ASR：`好了吗`
  - 回复：`已经准备好啦，你是想继续聊聊喜欢的蓝色相关的事，还是有别的想分享的呀？`
  - 播放完成回执绑定原 audio_id。
  - 临时 owner 下仅持久化 1 条带 session/turn ID 的轮次。
  - 客户端和临时服务均正常退出。

修复过程中另有多轮声学成功，也出现过一次 75 秒识别超时；该次清理已正常完成。
因此自动声学链路和退出问题已验证修复，但环境识别文本仍有波动。
`CARRIER-05` 要求的真人连续 3 轮对照仍标阻塞，不能用自动回采代替。

详见 [live-acoustic.md](validation/mvp-20260916-02/live-acoustic.md) 和
[audio-smoke.json](validation/mvp-20260916-02/audio-smoke.json)。

## 浏览器与数据

- 本轮隔离 API 和 Vite 服务均返回 200，但浏览器桥连续三次超时，未生成新的交互证据。
- 本轮未修改前端源码；四组前端自动探针和生产构建已重跑通过。
- 仅对前端未变部分引用上一轮桌面浏览器证据，其他真实 H5/移动端场景继续阻塞。
- 测试均使用临时 owner、base、figure、credential 和数据目录。
- 真实运行数据仍为 646 个文件；本轮 manifest 与修复前 manifest 的元数据数组完全一致。
- 原数据元数据摘要保持
  `d0c093a91cecb31dc43b4a3ee5d40bf915742335ba784c5a37f51963604f0959`。
- 所有临时 uvicorn、Vite、载体和探针进程均已停止。

## 剩余阻塞

- `E2E-05`：受控取消、旧帧隔离和重连退避已通过；仍缺 T3 三轮对话加断网
  30 秒恢复的综合录音和日志。
- `CARRIER-05`：自动声学脚本已成功；仍缺真人连续 3 轮对照。
- 其余阻塞主要涉及真实 H5 麦克风/WS、iOS/Android、USB 热插拔、权限拒绝、
  DNS/TLS/慢链路、systemd/launchd 冷启动及最终硬件。

阻塞不是失败，也不是通过。完成这些环境验收前，整体 MVP 结论保持“未通过”。
