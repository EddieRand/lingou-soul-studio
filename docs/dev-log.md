# 开发日志

---

## Day 1 · 项目初始化

**日期**：2026-06-11

**完成内容**：
- 项目整体架构确定：FastAPI 后端 + React H5 前端
- 数据模型设计（Figure / Base / Event / DialogueLog）
- 本地 JSON 存储层搭建
- Git 仓库初始化

**关键决策**：
- 采用 FastAPI 作为后端框架，利用其异步能力
- 前端使用 React + Tailwind CSS + Vite
- 存储层使用 JSON 文件，便于调试和演示

---

## Day 2 · 灵偶创建 API

**日期**：2026-06-12

**完成内容**：
- 灵偶（Figure）创建 API 开发
- Archetype 模板驱动机制（灵魂原型）
- `soul_profile` 结构设计
- `emotion_state` 6维情绪向量初始化
- H5 Soul Studio 页面框架搭建

**关键结构**：

```python
# soul_profile 结构
{
  "archetype": "yujie_care",
  "personality_traits": [...],
  "backstory": "..."
}

# emotion_state 6维
{
  "joy": 0.5,
  "anger": 0.0,
  "sadness": 0.0,
  "fear": 0.0,
  "surprise": 0.0,
  "disgust": 0.0
}
```

---

## Day 3 · 豆包 Ark 集成

**日期**：2026-06-13

**完成内容**：
- 豆包 Doubao Ark 在线大脑集成
- `brain_router` 路由规则设计
- 实时关键词/短文本 → 离线语音池判断逻辑
- Touch Response Engine 开发

**路由规则**：
- 关键词匹配（你好、再见等）→ 离线语音池
- 短文本（<10字）→ 离线语音池
- 复杂问句 → 豆包 Ark

---

## Day 4 · 火山 TTS 集成

**日期**：2026-06-14

**完成内容**：
- 火山引擎 TTS 流式合成接入
- Figure × 11 Archetypes × 10 音色映射矩阵
- `voice_profile` 结构设计
- 音色预览 API 开发

**关键映射**：每种 archetype 对应多种兼容音色，前端可自由选择搭配

---

## Day 5 · 前端全流程串联

**日期**：2026-06-15

**完成内容**：
- 创建 → 绑定 → 交互 → 对话 全流程串联
- 情绪系统 6 维展示与更新
- LED 灯光效果控制
- 交互页面与硬件模拟器

**完成页面**：
- CreateSoulPage（创建灵偶）
- SoulDetailPage（灵偶详情）
- HardwareSimPage（硬件模拟）

---

## Day 6 · Bug 修复与离线语音池

**日期**：2026-06-16

**完成内容**：
- Bug 修复：BV700_V2_streaming 型号名称替换
- 离线语音池（Precache + Playback）开发
- `voice_pool` 58句文本设计
- 架构口子预留：voxcpm2 / ASR adapters
- `tts_engine` 字段写入响应

**离线语音池**：
- 每灵偶预合成 58 句核心交互语音
- 按情绪状态匹配播放
- 支持即时播放，延迟 < 100ms

---

## Day 7 · MVP 收官

**日期**：2026-06-17

**完成内容**：
- H5 视觉打磨：梦幻紫 / 粉 / 蓝 渐变主题
- CreateSoulPage + SoulDetailPage 细节优化
- README 文档编写
- API 接口文档整理
- Demo 演示脚本录制
- MVP 功能验收

**最终验收项**：
- ✅ 灵偶创建（3步引导）
- ✅ 底座绑定与激活
- ✅ 触摸交互响应
- ✅ 在线/离线大脑切换
- ✅ 语音合成播放
- ✅ 情绪状态展示

---

## Day 8 · H5 全页面视觉重构

**日期**：2026-06-23

**完成内容**：
- H5 主流程页面统一升级为年轻化、品牌化的游戏 App 视觉
- `/home` 改为灵偶收藏大厅，新增当前灵偶 Hero、收藏槽位卡片和召唤 CTA
- 绑定、互动、对话、音色、创建、编辑、羁绊详情页统一紫粉玻璃质感和移动端布局
- 通用 Header、底部导航、玻璃卡片、按钮、Chip 视觉同步升级

**约束保持**：
- 只调整视觉结构与 Tailwind 样式
- 未新增依赖
- 未改动接口、业务逻辑、点击路径和数据渲染来源

---

## Day 9 · H5 Liquid Glass 交互质感

**日期**：2026-06-24

**完成内容**：
- 引入 `liquid-glass-react@1.1.1`，用于 Apple Liquid Glass 风格的折射、边缘高光和弹性质感
- 新增 `LiquidGlassPanel` 通用封装，统一控制移动端液态玻璃参数、圆角、折射强度和按压反馈
- Header 返回控件、底部导航、首页当前灵偶 Hero、快捷入口、绑定底座状态卡、互动对象卡、音色实验室主卡、对话当前灵魂卡、羁绊主页立绘卡、创建/编辑核心输入卡接入 Liquid Glass
- 全局 `glass-card` / `glass-card-light` 补充柔和高光、内阴影和半透明折射层，和原紫粉品牌风格保持一致

**兼容说明**：
- `liquid-glass-react` 的 peerDependencies 标注 React >= 19，但包源码仅使用 React 18 已支持的 Hook；当前 H5 保持 React 18.3.1，并在 H5 子项目 `.npmrc` 固化 `legacy-peer-deps=true`
- 未改动接口、路由、业务状态机、点击路径和数据渲染来源
- 已通过 `tsc --noEmit` 与 `npm run build`

**稳定性修复**：
- 修复 Liquid Glass 背景层内部 overlay 参与布局，导致页面出现大面积空白、灰色遮罩和闪烁的问题
- 移除尺寸变化时反复 remount 的逻辑，避免液态玻璃层持续重建
- 将 `liquid-glass-react` 折射层调整为 screen 高光层，深色信息卡改用稳定实体渐变，确保文字对比度
- 已用移动端截图验收 `/home`、`/bind`、`/simulate`、`/dialogue-debug`、`/voice-select`、`/create`、`/soul/:id`、`/soul/:id/edit`

**质感强化**：
- 按 Demo 感明显的方向强化 Liquid Glass：提高折射强度、色散边缘、弹性参数和高光层透明度
- `LiquidGlassPanel` 新增指针/触摸位置驱动，支持移动端按压和滑动时产生更明显的液态形变反馈
- 页面背景补充可被玻璃折射的光斑、星点和渐变纹理，解决纯浅色背景下“玻璃没东西可折射”的问题
- 主 CTA、底部导航、返回按钮、Hero 卡和品牌信息卡改为强液态玻璃层，普通内容卡仍保持柔和玻璃，控制视觉负载

**高级感回收**：
- 复盘强 Demo 方案后回撤过重的彩色条带、星点噪声、强色散和过亮边框，避免廉价炫光感
- Liquid Glass 参数改为中低折射、柔和色散、轻弹性，只在关键控件保留可感知的液态交互
- 背景恢复低噪声紫粉雾面渐变，星光粒子降密度、降透明度、放慢闪烁
- 首页空状态虚线框改为柔和内层玻璃，整体回到 C 端消费 App 的干净高级感

**Apple 清透玻璃版**：
- 根据移动端截图复盘，确认旧粉紫底色、厚白雾面、实色图标底和高饱和光斑会被 Liquid Glass 折射放大，导致卡片显脏、不透
- 全局背景改为冷白、银蓝、浅紫的低噪声空间底，紫色品牌色收缩为文字、状态和关键操作点缀
- `LiquidGlassPanel` 调整为更高边缘位移、更低雾化遮罩、更克制色差和更透明的玻璃填充，突出边缘折射与清透感
- 首页快捷入口、底部导航、Header、绑定页、互动页、对话页、音色页、创建/编辑页、羁绊详情页同步替换厚粉紫光斑和旧玻璃卡片
- 业务接口、路由、点击路径、状态管理和数据渲染逻辑保持不变；已通过 `tsc --noEmit` 与 `npm run build`

**水滴状 Liquid Glass 强化**：
- 背景回调为低噪声淡紫色，保持品牌紫调，同时避免旧版粉紫雾面导致玻璃显脏
- 新增 `liquid-droplet` 水滴控件样式，提供不规则圆角、凸透镜高光、底部焦散、内折射阴影和按压形变
- 首页主 CTA、快捷入口图标、底部导航选中态、Header 返回键、品牌胶囊、互动触摸按钮和选中 Chip 接入水滴化效果
- 移除按钮/Chip 默认方形 focus 边框，避免破坏水滴玻璃轮廓；仍保留原有点击、路由和数据渲染逻辑
- 复盘后将水滴外轮廓从不规则形态改为规则圆形/胶囊，水滴质感改由内部高光、焦散和折射阴影表达，避免控件显得变形
- 底部 Tab 当前水滴新增长按拖拽切换：长按当前 Tab 后可横向滑到目标 Tab 松手切换，普通点击路由保持原逻辑
- 复盘拖拽验证后，将底部 Tab 改为独立水滴指示器滑动，图标与文字固定在原位，避免拖拽时整个 Tab 内容跟随移动

**成熟产品化角色 UI 重构**：
- 参考 Grok 角色卡列表与 Docki Crew Companion App 的信息架构，将 `/home` 从功能列表重构为“当前出战 Hero + 快捷指挥入口 + 2 列灵偶角色卡”
- 灵偶卡片新增深紫舞台、角色视觉区、等级/出战状态、羁绊进度和主操作按钮，优先使用 `avatar_url`，无头像时使用 CSS 角色剪影 fallback
- `/soul/:id` 羁绊主页从重复信息卡堆叠重构为 Companion Profile：角色舞台、Soul Voice、关系等级、羁绊进度、解锁状态、记忆片段、唤醒词/音色、调试工具、对话与事件时间线
- 继续保留淡紫色主背景和 Liquid Glass 控件体系，深色角色舞台只作为角色内容承载层，避免整站变成纯黑
- 未改动接口、路由、业务状态、数据来源和底部 Tab 长按拖拽逻辑；已用移动端截图验收 `/home` 与 `/soul/:id`
- 回修无头像人物卡：移除字形/徽章式占位，改为 CSS 半身角色剪影、五官、刘海、肩颈和玻璃背景；首页列表前置，快捷入口后移，让人物卡在首屏更早出现
- 更新“春日部姐姐”头像素材：新增 `/avatars/chunribu-jiejie.png` 静态资源，并将灵偶数据 `avatar_url` 指向该头像；首页当前出战和羁绊主页已截图验证真实图片加载
- 更新“蜡笔小新”头像素材：新增 `/avatars/labixiaoxin.png` 静态资源，并将对应灵偶数据 `avatar_url` 指向该头像；同时将羁绊主页 Soul Voice 气泡改为浅玻璃底深色字，适配浅色头像背景
- 修复首页灵偶卡片可读性：将姓名、人格、Affinity、操作按钮和连续陪伴信息收进独立深紫玻璃信息面板，避免文字直接压在复杂头像上；已用超长移动端截图验证蜡笔小新和春日部姐姐卡片文字清晰
- 修复 H5 构建检查暴露的语音克隆类型问题：统一 `apiVoice.generate` 调用签名，补齐创建页克隆状态枚举，并修复声音克隆 JSX 分支闭合，保证 `tsc` 与生产构建通过
- 优化首页灵偶图卡清透度：将 2 列角色卡拉长为立牌比例，并把底部深紫信息面板改为透明液态玻璃样式，降低暗遮罩和彩色雾面，让角色图更完整可见，同时保留姓名、羁绊、进度与操作按钮的可读性；已用 390px 移动端截图复查
- 清理灵偶数据：仅保留悟空、春日部姐姐、熊二、蜡笔小新 4 个灵偶，删除其余灵偶 JSON，并同步过滤非保留灵偶的对话日志、事件记录、同步队列、语音缓存和 voice pool 目录；同时修正春日部姐姐与蜡笔小新内部档案里的旧测试名
- 更新悟空与熊二头像素材：新增 `/avatars/wukong.png`、`/avatars/xionger.png` 静态资源，并将对应灵偶数据的 `avatar_url` 与 `soul_profile.avatar_url` 指向新图
- 同步互动与对话页灵偶头像：新增复用的 `SoulFigureStage` 舞台组件，互动控制台、灵魂对话页的当前灵偶卡与灵偶切换列表均展示对应头像；对话记录里的灵偶回复也预留头像位，保持和羁绊详情页一致的角色呈现
- 固定返回按钮：将通用 `PageHeader` 的返回按钮改为视口固定定位，适配顶部安全区，覆盖创建、编辑、详情、互动、对话、音色等所有使用通用返回头的页面；滚动截图验证下拉后按钮仍停留在左上角
- 修复羁绊主页 Hero 卡文字对比：将顶部标签、人格类型、Mood 胶囊与次级操作按钮改为有效的高对比白色文字和更清晰的玻璃底，避免深紫背景上出现默认深色文字导致不可读
- 重构羁绊关系面板：将原统计表格式模块改为羁绊能量卡，新增等级徽章、液态能量轨道、玻璃仪表数据牌和状态解锁标签，提高移动端信息层级与文字对比度
- 核心页面自查后继续优化：增加主 Tab 页面底部安全区，避免底部导航遮挡内容；互动页收敛为紧凑控制台；对话页将大脑模式/API Key 收进调试折叠区；音色页补齐当前声线卡并默认选中灵偶；绑定页改为扫码绑定优先；创建/编辑页统一轻量固定操作条与头像预览
- 修复首页当前出战 Hero 对比：将统计格和次级操作按钮从蓝紫描边改为白色折射玻璃边、深紫内阴影和高光层，避免控件边界与深色舞台背景融在一起
- 编辑页新增灵偶图片更换：上传本地图片后前端自动裁剪为 4:5 立牌比例并压缩为 H5 展示图，保存时同步更新 `avatar_url` 与 `soul_profile.avatar_url`，让首页卡片、羁绊页、互动页和对话页统一使用新图

---

## Day 10 · 沉浸式登录页与 UI 细节精修

**日期**：2026-06-25

**完成内容**：
- 重构 H5 `/login` 为“沉浸式封面 → 点击唤醒 → 底部玻璃登录面板”的三态登录体验：`cover`、`loginSheet`、`loggingIn`
- 使用提供的灵偶封面图作为真实背景资源，保留 `background-size: cover`、`background-position: center top`，并补充轻微背景呼吸、星光闪烁和半透明蝴蝶漂浮动效
- 登录面板仅保留微信登录、Apple 登录和协议勾选；未勾选时 toast 提示，勾选后进入 mock 登录 loading，点击面板外背景可回到封面态
- 登录页按移动端优先实现 `100svh`、safe-area 底距、无黑边、无横向滚动；已用 390×844 与 430×932 截图验证
- 修正桌面浏览器横屏铺满问题：新增手机竖屏画布容器，桌面环境下固定为 430px 宽并居中展示，登录面板、toast 和底部提示都约束在手机画布内
- 全局主 Tab 页面底部安全区继续加大，底部导航外壳改为 safe-area 固定底距，减少滚动内容被导航遮挡
- 首页人物卡继续拉长图卡比例，弱化暗遮罩，增强透明液态玻璃信息浮层的文字对比
- 对话页、音色页、羁绊详情页进一步隐藏或弱化 `idle`、`AUTO`、ASR/TTS、speaker id 等调试/接口字段，改为 C 端用户能理解的文案
- 重构 `/create` 新增灵偶流程为对话式“唤醒仪式”：中央固定手办底座舞台，按气泡依次收集名字、可选立绘、一句话设定，再调用 `apiCharacter.generate` 自动预填人格、音色、触摸台词和深度档案
- 将原强制人格/音色/触摸配置降级为“细节微调”折叠入口，保留底座绑定校验、唤醒名生成、touch_escalation、保存与绑定当前底座逻辑
- 接入提供的 `lingou_figure_soul_base.svg`，并生成透明清理版 `/lingou_figure_soul_base_clean.png` 用作创建页中央手办示意图，避免透明棋盘格出现在页面中
- 修正创建页移动端适配：页面强制为手机竖屏画布，最大宽度 430px，高度 `100svh`，聊天区独立滚动；390×844 下 `scrollWidth === innerWidth`

**验证**：
- `npm run build` 通过
- 自动化截图验证 `/login` 初始封面、登录面板、未勾选协议 toast、勾选后 mock logging、面板外点击关闭
- 390×844 与 430×932 下 `scrollWidth === innerWidth`，确认无横向滚动
- 自动化截图验证 `/create` 初始唤醒页与契约揭晓页，390×844 下无横向滚动，完整创建流程可走到保存入口

---

## Day 11 · 首次登录新手引导

**日期**：2026-06-26

**完成内容**：
- 新增 `/onboarding` 首次登录 3 步引导页：绑定底座、创建 Soul Profile、叫名字/触摸底座
- 使用提供的三张设计稿原图作为整屏背景资源，叠加透明跳过、下一步、开始创建点击热区，保证比赛 DEMO 阶段 1:1 视觉还原
- 登录成功后按 `lingou_onboarding_done` 本地标记分流：未完成引导进入 `/onboarding`，跳过或完成后进入 `/home` 或 `/create`
- 引导页按移动端优先实现 `100svh`、淡紫底色兜底透明圆角、`object-fit: cover`、无横向滚动
- 同步更新 `PLAN.md`，将首次登录新手引导标记为已完成，并保留比赛 H5 DEMO 上线为当前进行项

**约束保持**：
- 未新增依赖
- 未改动后端接口、灵偶数据结构、登录 API 或业务数据渲染逻辑
